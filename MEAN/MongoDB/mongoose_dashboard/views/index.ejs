<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>MongoDB - Mongoose Dashboard Assignment</title>
    <style>
      .mongoose{
        text-align: center;
        display: inline-block;
        width: 100px;
        background-color: LightBlue;
        border-radius: 10px;
      }
      .links{
        background-color: Peru;
        border-bottom-right-radius: 10px;
        border-bottom-left-radius: 10px;
      }
      .links a{
        text-decoration: none;
        color: white;
      }

      .links a:hover{
        color: black;
      }
    </style>
  </head>
  <body>
    <h3>The Mongeese</h3>
    <% for (var idx in mongeese) {%>
      <div class="mongoose">
        <p><%= mongeese[idx].name %></p>
        <div class="links">
          <a href="/mongooses/edit/<%= mongeese[idx]._id %>">Edit</a> <span>|</span>
          <a href="/mongooses/destroy/<%= mongeese[idx]._id %>">Delete</a>
        </div>
      </div>
    <% } %>
    <p><a href="/mongooses/new">Add a new Mongoose</a></p>
  </body>
</html>
